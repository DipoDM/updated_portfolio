name: portfolio
description: Oladipo Danmusa's Personal Portfolio - Flutter Web

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^12.1.3
  flutter_riverpod: ^2.4.9
  google_fonts: ^6.1.0
  visibility_detector: ^0.4.0+2
  printing: ^5.11.1
  pdf: ^3.10.7
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - web/resume.pdf
